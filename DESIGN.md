# 空中鬼ごっこ（Sky Tag）設計書

> この文書は Claude Code に実装を依頼するための設計書です。
> リポジトリ直下に `DESIGN.md` として置き、「DESIGN.md のフェーズ1を実装して」のように段階的に依頼してください。

---

## 0. Claude Code への作業ルール

- 実装は **フェーズ単位** で進める（セクション9）。1フェーズ完了ごとに受け入れ条件を満たしているか確認し、テストを通してから次へ進む。
- **ゲームロジック（`src/sim/`）は描画（Three.js）に一切依存させない。** Node.js 上でヘッドレス実行できる状態を常に保つ。これはAI学習・自動テスト・将来のオンライン対戦の土台になる。
- シミュレーションは **固定タイムステップ（60Hz）**、乱数は **シード付きRNG** のみ使用する（`Math.random()` 禁止）。
- ゲームバランスに関わる数値はすべて `src/sim/config.ts` に集約し、コード中に直書きしない。
- 各フェーズで `npm run test` と `npm run typecheck` が通ること。

---

## 1. ゲーム概要

鬼（ハンター）と逃亡者（ランナー）が1対1で、3次元の都市マップ上空を飛び回りながら光線銃で撃ち合う鬼ごっこ。

| 項目 | 内容 |
|---|---|
| 人数 | 1対1 |
| 視点 | 三人称（機体の後方追従カメラ） |
| 対戦モード | CPU対戦 / ローカル2人対戦（画面分割） / オンライン対戦（後期フェーズ） |
| プラットフォーム | Webブラウザ（PC、キーボード＋マウス / ゲームパッド） |

---

## 2. ルール

### 2.1 勝利条件

| 陣営 | 勝利条件 |
|---|---|
| 鬼 | ①光線銃で逃亡者のHPを0にする　②逃亡者にタッチする（接触判定） |
| 逃亡者 | ①光線銃で鬼のHPを0にする　②制限時間まで生き残る ※提案 |

> **バランス上の注意（提案）**：元のルールでは鬼の勝ち筋が2つ、逃亡者が1つで鬼有利になりやすい。そのため「制限時間まで逃げ切れば逃亡者の勝ち」を追加することを推奨する。`config.ts` の `timeoutWinner` で切り替え可能にすること（`"runner" | "draw"`）。

### 2.2 試合の流れ

1. マップ上の離れた2地点にスポーン（距離は最低150m）
2. カウントダウン3秒後に開始
3. 勝敗が決まるか制限時間で1ラウンド終了
4. **2ラウンド先取（Best of 3）**。ラウンドごとに鬼と逃亡者を交代するオプションあり

### 2.3 初期パラメータ（すべて `config.ts` で調整）

| パラメータ | 鬼 | 逃亡者 | 備考 |
|---|---|---|---|
| 最大HP | 100 | 100 | |
| 光線銃ダメージ | 8 | 10 | 逃亡者はタッチ勝ちが無い分やや高め |
| 連射間隔 | 0.2秒 | 0.2秒 | |
| オーバーヒート | 20連射で3秒冷却 | 同左 | 撃ちっぱなし防止 |
| 射程 | 120m | 120m | ヒットスキャン（即着弾のレイ判定） |
| 巡航速度 | 22 m/s | 22 m/s | |
| ブースト速度 | ×1.8 | ×2.0 | 逃亡者は逃げ性能が少し上 |
| ブーストゲージ | 100、消費30/秒、回復15/秒 | 同左 | |
| タッチ判定半径 | 1.8m | ― | 機体中心間の距離 |
| 制限時間 | 180秒 | | |
| 障害物への衝突 | 停止＋小ダメージ5、0.5秒操作不能 | 同左 | 雑な飛行へのペナルティ |

---

## 3. 操作

| 操作 | キーボード＋マウス | ゲームパッド |
|---|---|---|
| 前後左右移動 | W / A / S / D | 左スティック |
| 上昇 / 下降 | Space / Ctrl | RB / LB |
| 視点・照準 | マウス | 右スティック |
| 射撃 | 左クリック | RT |
| ブースト | Shift | LT |

- ローカル2人対戦時は「P1：キーボード＋マウス、P2：ゲームパッド」または「2台のゲームパッド」。
- ゲームパッドには弱めのエイムアシスト（照準中心から5°以内の敵へ緩やかに吸着）を入れる。`config.ts` でON/OFF。

---

## 4. マップ

### 4.1 構成

- サイズ：400m × 400m、高度上限 150m（上限と外周は見えない壁）
- **地上層（0〜30m）**：地形の起伏（ハイトマップ）、低層ビル、トンネル、高架橋、木々。隠れる場所が多く、タッチを狙いやすい/狙われやすいエリア
- **中層（30〜90m）**：高層ビル群、ビル間の渡り廊下、クレーン。遮蔽物を使った撃ち合いのメインエリア
- **上空（90〜150m）**：浮遊する障害物（気球、浮島など）が少しある開けた空間。見通しが良く射撃戦になりやすい

### 4.2 データ形式

- マップは `maps/*.json` で定義（地形のハイトマップ設定、建物の位置・サイズ、浮遊物、スポーン候補地点）
- 当初の見た目は **単純な形状（ボックス、シリンダー）＋色分け** で十分。アセット制作は後回し
- シード値から建物配置を自動生成する `generateCityMap(seed)` も用意し、AIの汎化テストに使う

---

## 5. 技術スタック

| 用途 | 採用技術 | 理由 |
|---|---|---|
| 言語 | TypeScript | 型でゲームロジックの破綻を防ぐ |
| ビルド | Vite | 高速、設定が軽い |
| 3D描画 | Three.js | Web 3Dの定番、情報量が多い |
| 物理・当たり判定 | Rapier（`@dimforge/rapier3d-compat`） | ブラウザでもNodeでも動く、レイキャストが高速 |
| テスト | Vitest | |
| オンライン対戦（後期） | Node.js + WebSocket（`ws`） | `src/sim/` をそのままサーバーで流用 |
| 強化学習（任意・最終フェーズ） | Python + Stable-Baselines3 → ONNX → `onnxruntime-web` | |

> Unity + ML-Agents という選択肢もあるが、Unityはエディタ操作が必要で Claude Code が直接扱いにくいため、コードだけで完結するWeb構成を採用する。

---

## 6. アーキテクチャ

### 6.1 ディレクトリ構成

```
sky-tag/
├── DESIGN.md
├── src/
│   ├── sim/                 # ゲームロジック（描画非依存・ヘッドレス実行可能）
│   │   ├── config.ts        # 全バランス数値
│   │   ├── world.ts         # World: 状態保持と step(dt, inputs)
│   │   ├── physics.ts       # Rapier ラッパー
│   │   ├── flight.ts        # 飛行制御（速度・ブースト・衝突ペナルティ）
│   │   ├── weapon.ts        # 光線銃（ヒットスキャン、オーバーヒート）
│   │   ├── rules.ts         # 勝敗判定、タッチ判定、ラウンド管理
│   │   ├── rng.ts           # シード付き乱数
│   │   └── types.ts         # PlayerInput, EntityState など
│   ├── maps/
│   │   ├── loader.ts
│   │   └── generator.ts     # generateCityMap(seed)
│   ├── render/              # Three.js 描画のみ（sim の状態を読むだけ）
│   │   ├── scene.ts
│   │   ├── camera.ts        # 追従カメラ、画面分割
│   │   ├── effects.ts       # ビーム、被弾、爆発
│   │   └── hud.ts           # HP、ゲージ、残り時間、敵方向インジケーター
│   ├── input/
│   │   ├── keyboardMouse.ts
│   │   └── gamepad.ts
│   ├── ai/
│   │   ├── controller.ts    # AI も PlayerInput を出すだけ（人間と同じ口）
│   │   ├── perception.ts    # 視線判定、最終目撃位置
│   │   ├── nav/
│   │   │   ├── voxelGrid.ts # 3D空間のボクセル化（通行可能判定）
│   │   │   └── pathfind.ts  # 3D A*
│   │   ├── behavior/
│   │   │   ├── hunterBrain.ts
│   │   │   └── runnerBrain.ts
│   │   └── learning/
│   │       ├── playerModel.ts   # プレイヤー行動の統計モデル
│   │       └── storage.ts       # 学習データの保存/読込
│   ├── net/                 # フェーズ6
│   ├── ui/                  # タイトル、モード選択、リザルト
│   └── main.ts
├── server/                  # フェーズ6
├── tools/
│   ├── simBatch.ts          # AI同士をヘッドレスで大量対戦させ勝率を出す
│   └── rl/                  # フェーズ7（Python）
└── tests/
```

### 6.2 中核の設計原則

1. **入力の統一**：人間・CPU・ネットワーク越しの相手、すべて同じ `PlayerInput` 型をシミュレーションに渡す。

```ts
interface PlayerInput {
  move: { x: number; y: number; z: number }; // -1..1（左右・上下・前後）
  aimYaw: number;   // ラジアン
  aimPitch: number; // ラジアン
  fire: boolean;
  boost: boolean;
}
```

2. **シミュレーションの純粋性**：`world.step(inputs)` は同じ初期状態・同じ入力列・同じシードなら必ず同じ結果になる（リプレイ、テスト、学習で使う）。
3. **描画は読み取り専用**：`render/` は `world` の状態を読んで補間描画するだけ。状態を書き換えない。

---

## 7. CPU（AI）設計

AIは3段階で賢くする。**段階A・Bだけでも「学習して効率的に動くCPU」として十分成立する**。段階Cは任意。

### 7.1 段階A：ルールベースAI（学習なし）

**3Dナビゲーション**
- マップを4m単位のボクセルに分割し、障害物と重ならない空間を「通行可能」とする
- 3D A* で経路を探索し、経路を平滑化して滑らかに飛ぶ
- 経路探索は重いので0.5秒ごと、または目標が大きく動いたときだけ再計算

**知覚**
- 相手が視界（前方120°）内かつ遮蔽物なし（レイキャスト）なら「目撃」
- 見失ったら「最終目撃位置」と「最終速度」を記憶する（人間と同じく壁の向こうは見えない＝ずるをしない）

**行動（ユーティリティAI：各行動をスコア化し最大のものを選択）**

| 鬼の行動 | 内容 |
|---|---|
| 捜索 | 最終目撃位置の周辺を探す |
| 追跡 | 経路探索で接近、タッチを狙う |
| 射撃戦 | 距離を保ちつつ偏差射撃（相手の移動先を予測して撃つ） |
| 待ち伏せ | 相手が通りそうな地点の近くの遮蔽物に潜む |

| 逃亡者の行動 | 内容 |
|---|---|
| 退避 | 鬼から遠く、遮蔽物の多い方向へ逃げる |
| 隠れる | 鬼から視線が通らない位置へ移動 |
| 反撃 | 鬼のHPが低い/自分が有利な距離のとき撃ち合う |
| 引き撃ち | 距離を取りながら後ろへ射撃 |

**難易度**：反応時間（0.15〜0.6秒）、照準の誤差、偏差射撃の精度で調整（Easy / Normal / Hard）。

### 7.2 段階B：プレイヤー学習（対戦を重ねるほど賢くなる）

対戦中のプレイヤーの行動を記録し、統計モデルとして蓄積する。重い機械学習は使わず、軽量でブラウザ内で完結する。

| 学習する内容 | 記録方法 | AIでの使い道 |
|---|---|---|
| よく居る場所 | マップをエリア分割し滞在時間のヒートマップ | 見失ったときの捜索優先度、待ち伏せ地点の選定 |
| 移動ルートの癖 | エリア間の遷移確率（マルコフ連鎖） | 見失った後の移動先予測 |
| 回避の癖 | 被弾しそうなとき（射線に入ったとき）の回避方向の分布 | 偏差射撃の補正（避ける方向に撃つ） |
| 好む交戦距離 | 射撃時の相手との距離の分布 | 相手が得意な距離を避けて戦う |
| 高度の好み | 時間帯ごとの高度分布 | 捜索や待ち伏せの高さを調整 |
| 射撃精度 | 命中率 | 精度が高い相手には遮蔽物を多用 |

- **マップ自体の学習**：AI同士のヘッドレス対戦（`tools/simBatch.ts`）で「交戦が起きやすい場所」「逃げ切りやすいルート」を事前集計し、マップごとのデータとして同梱する
- 学習データはマップ別・プレイヤー別に保存（ブラウザでは IndexedDB）
- 古いデータの影響は徐々に減衰させる（最近の傾向を重視）
- 設定画面に「学習データをリセット」ボタンを置く
- HUD または リザルト画面に「CPUが学習した傾向」を簡単に表示すると面白さが伝わる（例：「あなたは右に避けがち」）

### 7.3 段階C：強化学習（任意・上級）

- ヘッドレスのシミュレーションをGym互換の環境として Python から操作できるようにする（Nodeでシミュを動かし、標準入出力かソケットで通信）
- 観測：自機・相手の相対位置/速度、HP、ゲージ、周囲の障害物距離（レイ数十本）
- 行動：`PlayerInput` をそのまま使う
- 報酬：勝利 +1 / 敗北 -1、命中・被弾で小さな加減点
- 鬼と逃亡者を交互に鍛える **セルフプレイ** で学習
- 学習済みモデルを ONNX で書き出し、`onnxruntime-web` でブラウザ実行
- 注意：報酬設計の試行錯誤と長い学習時間（数時間〜数日）が必要。段階Bで満足できるなら不要

---

## 8. 画面・UI

- タイトル → モード選択（CPU対戦 / ローカル対戦 / オンライン）→ 陣営・難易度・マップ選択 → 試合 → リザルト
- HUD：自分と相手のHP、ブーストゲージ、オーバーヒートゲージ、残り時間、画面外の相手の方向矢印（鬼側は最終目撃位置のみ表示などの非対称設定も可）
- 簡易レーダー（範囲や表示条件は `config.ts` で調整）

---

## 9. 実装フェーズと受け入れ条件

| フェーズ | 内容 | 受け入れ条件 |
|---|---|---|
| 1. 飛行の土台 | Vite+TS+Three.js+Rapier の環境構築、平らな地面と箱の建物、1機を飛ばせる、追従カメラ | ブラウザで機体を自由に飛ばせ、建物に衝突する。`sim/` がNodeで単体実行できる |
| 2. 対戦ルール | 2機、光線銃、HP、タッチ判定、オーバーヒート、ブースト、制限時間、ラウンド制、HUD | 同じPCでキーボード＋ゲームパッドの2人対戦が最後までできる。`rules.ts` のユニットテストが通る |
| 3. マップ | JSON マップ読込、地形の起伏、高層ビル・トンネル・浮遊物、自動生成 | 地上/中層/上空の3層を持つマップが1つ以上遊べる |
| 4. CPU（段階A） | ボクセル化、3D A*、知覚、ユーティリティAI、難易度 | CPUと対戦でき、Hardで平均的なプレイヤーに勝ち越せる程度に動く。`simBatch` でAI同士の勝率が出る |
| 5. 学習AI（段階B） | プレイヤーモデルの記録・保存・活用、マップ事前集計 | 同じプレイヤーと数戦すると捜索と偏差射撃の精度が上がる（ログで確認できる） |
| 6. オンライン対戦 | WebSocket のサーバー権威型、クライアント予測と補間、ルーム作成 | 別々のPCから同じ試合に参加でき、遅延100msで破綻しない |
| 7. 強化学習（任意） | Gym環境、セルフプレイ学習、ONNX 推論 | 学習済みCPUが段階BのCPUに勝ち越す |

### バランス調整の回し方

```
npm run sim:batch -- --matches 500 --map city01 --hunter hard --runner hard
```

AI同士の勝率・平均試合時間・勝因（HP/タッチ/時間切れ）の内訳を出力する。鬼の勝率が45〜55%に収まるよう `config.ts` を調整する。

---

## 10. 未決事項（実装前に決めたいこと）

- 時間切れ時の扱い（逃亡者勝ち / 引き分け）
- 鬼側に相手の位置がどこまで見えるか（常時表示 / 目撃時のみ）
- 被弾後の無敵時間の有無
- オンライン対戦を本当に作るか（作る場合はフェーズ1からサーバー権威を意識した作りにしてある）
- 見た目の方向性（ローポリ、トゥーン調など）
